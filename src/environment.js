const BASE_URL = 'https://blackisp.herokuapp.com';

const environment = {
    GET_DATA_CODE_POSTAL: `${BASE_URL}/postalCodes`,
    POST_DATA_CONTACT: `${BASE_URL}/contact`,
    GET_PRODUCTS: `${BASE_URL}/products`,
}

export default environment;